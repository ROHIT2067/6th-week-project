
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Admin Panel</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link rel="stylesheet" href="/adminlogin.css">
</head>
<body class="bg-light">

  <div class="container">
    <div class="row justify-content-center align-items-center" style="height:100vh;">
      <div class="col-lg-6 col-md-8 login-box shadow-lg p-4 rounded bg-white">
        
        <!-- Icon -->
        <div class="text-center mb-3">
          <i class="fa fa-key fa-3x text-primary"></i>
        </div>

        <!-- Title -->
        <div class="text-center mb-4">
          <h3 class="fw-bold">ADMIN PANEL</h3>
        </div>

        <!-- Login Form -->
        <form action="/admin/login" method="post">
          <div class="form-group mb-3">
            <label class="form-control-label">USERNAME</label>
            <input type="text" name="adminUsr" class="form-control">
            <p class="err-mess"></p>
          </div>
          <div class="form-group mb-3">
            <label class="form-control-label">PASSWORD</label>
            <input type="password" name="adminPass" class="form-control">
            <p class="err-mess"> <%= adminerr %> </p>
          </div>
          <div class="d-flex justify-content-between align-items-center mt-3">
            <div></div>
            <button type="submit" class="btn btn-outline-primary px-4">LOGIN</button>
          </div>
        </form>

      </div>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    <script>
  // Prevent going back to this page from cache after logout
  window.addEventListener("pageshow", function (event) {
    if (event.persisted) {
      // Force a full reload from the server instead of showing a cached version
      window.location.reload();
    }
  });
</script>

</body>

</html>
